// Update this once you've deployed the Pookie Dashboard web app to Vercel,
// or override it per-build with the EXPO_PUBLIC_WEB_URL env var.
export const WEB_APP_URL = process.env.EXPO_PUBLIC_WEB_URL || 'https://pookie-expenses.vercel.app';
